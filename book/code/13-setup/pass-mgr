brew install pass gpg2
# get your public key id
gpg2 --list-keys 
# now using id of your public GPG key
pass init C25565E4701F4ED36A0711AA114F3606EFD923BB
pass insert aws-access-key-id
pass ls
pass insert aws-secret-access-key
pass ls
pass show aws-secret-access-key

